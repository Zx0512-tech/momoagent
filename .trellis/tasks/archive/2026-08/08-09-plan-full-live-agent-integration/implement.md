# 规划交付清单

1. [x] 盘点五类 Agent、平台 JobType、前端能力目录和现有真实 `REAL_*` 路径。
2. [x] 定位 501、placeholder、固定 preview 和 `PENDING_REPLACEMENT` 分支。
3. [x] 核对 `pyansys_bridge` 可复用 batch/surrogate/active-learning/optimization 接口。
4. [x] 定义共享真实执行器目标架构和六阶段实施顺序。
5. [x] 为每阶段定义可观察验收、501 解除条件、场景支持矩阵规则和回滚门。

该子任务只交付路线图；后续每个 Phase 应创建独立 Trellis 实施任务，禁止合并成一次大范围重写。
