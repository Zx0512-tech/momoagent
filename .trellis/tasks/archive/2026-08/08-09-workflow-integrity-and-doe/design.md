# 技术设计

- 用 `WorkflowGuard.advance()` 逐步推进证据和报告门禁；失败路由保留未通过步骤，报告登记校验后再完成。
- 统一 DOE 常量并在审批 DTO、LLM 模型、冻结工作流、执行配置和证据输出中复用。
- 15 点优先使用既有显式模板；其他数量使用中心点、四角点和固定种子分层 LHS。
- 工具 spec 是参数、风险和审批元数据的单一事实源；内部调用先构造完整参数，再严格校验并原样记录。
- 旧工作流缺少失败路由时失败关闭并写 `workflowGateError`。
